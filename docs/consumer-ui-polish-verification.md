# 客户版界面精简与主题一致性验收

日期：2026-09-11。工作分支：`feat/formal-baseline-refinement`。
按 `consumer-ui-polish-plan.md` 执行；继续沿用已确认的正式版布局，三个底部导航及现有图片保持不变。

## 已完成

- 删除名片编辑的 AI 润色按钮、状态和调用；缩短重复说明，保留输入限制、错误反馈及必要的公开范围选项。
- 「我的」移除隐私管理、分享管理、待开放认证等非必要入口；保留自己的名片查看/编辑，以及 13 城社群和 ABclub1 联系方式。
- 客户分享流程移除转发测试、访客自测、手动令牌及二维码测试面板。编辑完成后的分享按钮直接使用微信原生转发；独立海报生成/保存仍可用。
- 复制微信号按钮采用淡香槟金半透明底、细边框及柔和高光，配置模糊效果与不支持模糊时的底色回退。
- 原生导航栏、底部导航、发现/活动/我的适配系统深色外观；名片编辑保留象牙白工作区并修正输入、选中项、操作按钮和成功反馈的对比度。
- 四种个人名片主题同步传入原生分享封面和独立保存海报。清理封面的重复装饰说明，不再为未填写的信息编造简介或身份。
- 字段过滤、分享失效处理和真实云端分享的停止分享能力未移除。

## 自动检查

`npm run build` 成功，包括小程序/云函数/合同类型检查、基础验证、测试和包大小检查。

- 基础验证：339 项通过。
- 测试：414 项，408 通过，0 失败，6 跳过。
- 跳过的 6 项是原有可选 miniprogram-simulate 组件执行组；本环境缺少 jsdom，不计为通过。真实开发者工具验收另列如下，不冒充这 6 项结果。
- `git diff --check` 通过。
- 独立只读复核：71 个已跟踪图片文件字节均未变化；云函数、共享权限合同、隐私过滤服务、依赖和私有配置无本轮变更。

## 实际微信开发者工具验证

运行环境：原生小程序，`OFFLINE_DEMO`，不是云端正式账号系统。合成测试人物为「林雅·验收」。

| 场景 | 结果 |
| --- | --- |
| 我的页面及管理入口移除 | READY；旧管理行数量为 0 |
| 复制微信号 | 实际点击后剪贴板值为 ABclub1；原剪贴板已还原 |
| 编辑、保存、完成页 | 合成称呼、简介及标签成功保存并呈现；无 AI 按钮 |
| 直接分享 | 实际按钮打开微信原生「虚拟好友」发送确认框；封面显示人物信息；取消发送，未向真实联系人发送 |
| 四主题封面 | 原生 Canvas 生成象牙白、墨黑、香槟金、石灰灰四份 1200×960 图片，内容和配色一致，文件哈希各不相同 |
| 访客接收 | 象牙白和墨黑分享路径实测 SUCCESS；人物称呼、简介及主题正确，不进入本机名片编辑 |
| 独立海报 | 实际生成成功，1280×1760；保存到相册的权限申请未在本轮实机执行 |
| 真实系统深色模拟 | 微信运行时 theme=dark；检查发现、活动、我的、象牙白编辑和预览的实际呈现 |
| 深色编辑对比度 | 象牙白工作区使用深色输入和反馈文字，无白字落在浅底的问题 |
| 收尾 | 本轮合成名片记录已移出模拟器，保留本地恢复副本；其他存储未批量删除。模拟器恢复 light，并停在「我的」页 |

验收辅助脚本曾因等待错误状态字段而超时；重连后读取接收页实际字段 `state=SUCCESS`。这不是产品失败，也没有把未完成的四主题接收循环记为四项通过。

## 编译、预览与上传

- Developer Tools preview：返回 `√ preview`，生成可扫码预览二维码。
- 上传版本：**1.4.7**，返回 **`√ upload`**，进程退出码 0。
- 更新说明：精简客户版入口，移除AI润色，统一封面与深色样式，优化微信号复制按钮。
- 上传总包 2,227,357 字节，主包 1,893,626 字节；其余分包均低于工具限制。预览及上传回执一致。
- 这是上传至微信后台的开发版本；未提交审核、未发布线上正式版，未宣称已设置体验版或完成两台真机账号互发。

## 本地证据

证据目录：`E:\Temp\abclub-formal-refinement-verification`（不随源码上传）。

- `build-consumer-polish.log`
- `consumer-final-my.png`、`consumer-me-dark.png`
- `consumer-editor-dark-os.png`、`consumer-editor-actions.png`
- `consumer-native-share-panel.png`
- `consumer-cover-{ivory,ink,champagne,stone}.png`
- `consumer-saved-poster.png`
- `preview-consumer-info.json`、`preview-consumer-qr.png`
- `upload-info-1.4.7.json`

## 保持的边界

当前本机演示模式仍不把本机电话、邮箱加入公开快照，即使本机显示开关打开；这些既有隐私规则没有被界面精简绕过。上传代码不包含本机名片数据、私有 AppID 配置或密钥。正式云端分享、电话/邮件跨设备公开及真实双账号投递须单独验收。
