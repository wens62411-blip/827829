# 活动三入口：实施与验收记录

日期：2026-09-13（Asia/Shanghai）。状态：LOCAL_BUILD_PASS / LOCAL_SIMULATOR_PARTIAL / NOT_UPLOADED。

## 实施范围

- 活动一级入口顺序：全球艺术日历 / GLOBAL ART CALENDAR → 公益 / PUBLIC GOOD → 珍珠展示 / SPIRIT PEARLS。底部导航仍为发现、活动、我的。
- 年历：独立维护 2026 年 20 项内容、12 个月、7 项重点事件；按月内日期排序、可重复点击同一月份定位、事件简要信息弹窗。固定暖暗背景与金色细节，不开发票务或详细资讯页。
- 公益：附件的 100 条小事完整保留，10 类 × 10 条；随机抽取、换一件、完成、分类浏览、本地恢复。仅首次完成同一小事 +1，抽取不计分。独立存储键 abclub.public-good.v1，不接触名片存储。
- 珍珠：源头选珠、淡水珠 / Akoya / 澳白意向、耳钉 / 项链 / 戒指参考、原图放大、原生客服、复制 ABclub1 和图片来源展开。图片为参考，不承诺库存、产地鉴定、售价或可立即成交。
- 现有工作树改动被保留；没有 reset、提交、Git 推送、上传或发布。

## 自动化与文件保护

- `npm run build` 最终退出码 0，包含三个 TypeScript 范围、基础结构验证、全量测试与静态包体检查。
- 基础结构验证：339 通过，0 失败。
- 全量测试：414 项，408 通过，0 失败，6 跳过；跳过项属于已有 miniprogram-simulate 环境门禁及组件渲染，不冒充通过。
- 29 个注册页面均有完整源文件；主包和各分包静态预算通过。此结果不是微信服务端上传包体结果。
- 本轮曾发现原生 button 默认宽度和字重覆盖页面设计，已为活动入口、年历、公益清单和珍珠入口加入限定范围的布局覆盖，并重新查看模拟器截图。
- 名片、编辑、分享、我的和名片展示组件共 44 个文件，与开始前 SHA-256 清单逐字节一致。
- 主项目 356 个 miniprogram 文件与独立预览工程逐一哈希比对，差异 0。预览工程只在项目配置层填入用户此前提供的 AppID；不把账号私有配置写回共享源码。
- `git -c core.whitespace=cr-at-eol diff --check` 通过，没有批量转换既有文件换行符。

构建原始日志：`E:\Temp\abclub-culture-verification-20260912\build-final.log`。

## 开发者工具实际验收

独立预览目录：`E:\Temp\abclub-culture-preview-20260912`。微信开发者工具 2.02.2608040，基础库 3.17.2，实际已确认模拟器设备为 iPhone 12/13 Pro（390 × 844）。不使用 HTML 仿真图代替小程序运行截图。

| 检查 | 实际结果 |
| --- | --- |
| 活动三个入口 | 均进入各自已注册的二级页，无一级导航新增 |
| 年历月份跳转 | DEC 可达，滚动值 4238；手动回到顶部后再点 DEC 仍恢复定位；横向月份栏可滚到年末 |
| 年历配色 | 实际背景 rgb(36,33,30)，导航文字白色；年度内容和底部页脚可见 |
| 公益抽取 | 抽取后完成计数保持 0 |
| 公益完成 | 首次完成变为 1，重复完成不再次累计 |
| 公益恢复 | 离开、重进和重启预览工程后保留完成计数 1；分类可切换，10 条清单全宽显示 |
| 珍珠意向 | 带 category=akoya 进入恢复 Akoya；点击澳白后会话上下文更新为澳白 |
| 负责人复制 | 微信 API 返回 getClipboardData:ok，内容准确为 ABclub1，按钮恢复可用状态 |
| 原生客服点击 | 实际点击后出现官方提示“模拟进入客服会话；开发者工具暂不支持打开客服会话，请使用真机调试”，未发送任何消息 |
| 新模块运行异常 | 最后一轮已执行上述页面浏览/选择/复制期间，automator exception 监听结果为空；不等于整个开发者工具历史控制台零错误 |

开发者工具自动化兼容说明：现有 miniprogram-automator 0.12.1 的 connect 版本检查不适配新版 Tool.getInfo 返回结构；本次使用其已有 Connection / MiniProgram 实例连接，并单独读取实际基础库版本。没有修改依赖包或伪造版本检查结果。

## 尚未通过的项目

1. 澳白裸珠实拍缺少能够确认澳大利亚来源且有明确复用授权的图片。目前保留优雅占位“联系查看实拍”，意向选择及咨询可用；不能视为“三品类照片全部完成”。其余 5 张照片合计 705,146 bytes，来源与许可见 [图片来源](pearl-image-sources.md)。
2. 真机客服会话、后台客服实际接待、体验版上传和手机端验收均未完成；模拟器原生提示不证明后台已配置或客服已接通。
3. 公益和珍珠的系统暗色样式及窄屏约束已静态检查，主要文字对比度充足；本轮未成功完成系统主题切换及 320px 机型的实际截图验收，不把这些实测项标为通过。
4. 启动期开发者工具存在 AppID 环境读取异常；尽管预览配置和 CLI 确认使用指定 AppID，运行时 getAccountInfoSync 的 appId 仍为空。页面可本地浏览，但不据此推断真实账号能力可用，不改动现有名片逻辑来掩盖环境问题。

## 实际截图

截图目录：`E:\Temp\abclub-culture-verification-20260912`。

- `activity-light.png`：一级活动三入口。
- `calendar-top.png`、`calendar-december.png`：固定暖暗年历及年末定位。
- `public-good-current.png`、`public-good-library.png`：当前公益小事及全宽分类清单。
- `pearls-light.png`、`pearls-styles.png`、`pearls-contact.png`：珍珠品类、设计参考、联系区。
- `pearls-contact-result.png`：开发者工具原生客服限制提示。

所有测试中产生的公益进度都属于独立预览工程；未清理或覆盖用户现有名片及任何全局本地存储。
