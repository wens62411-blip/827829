# 1.4.5 经典白金优化 — 本地验收记录

日期：2026-09-11

## 基线与范围

- 工程：`E:/Temp/abclub-1.4.5-classic`
- 分支：`feat/1.4.5-classic-polish`
- 起点：`1fc79d85002364635989b49a4ead31e9506ba28f`（1.4.5），未继承被否决的 1.4.7 整体改版。
- 原工程保留，未回退或覆盖原目录。
- 保留发现、活动、我的三个底部入口及现有照片资源。
- 本轮为本地界面优化及预览，不代表上传、审核通过或正式发布。

## 实现

- 清理全部客户功能页面的开发者说明、运行模式横幅、重复技术描述；保留必要操作错误、示例人物/作品与未开放状态。
- 移除名片编辑 AI 润色入口与开发测试面板。
- 分享卡片添加细双线、对称边角与菱形装饰，小号 AB CLUB 与竖排姓名，保留公开资料及留白。
- 微信聊天卡片按象牙白、暖灰褐、香槟金、石灰灰四种名片主题生成；长海报复用同一配色和装饰。
- 系统深色背景改为暖灰；My 城市模块采用白金底色和淡金半透明按钮。
- 原生导航声明主题适配，个人自选名片主题不被系统主题覆盖。
- 保持原生直接分享、分享接收页与本人编辑入口隔离；没有修改云端合同或联系人过滤规则。

## 已验证

- 完整构建成功：TypeScript、339 项基础结构检查、静态包体检查通过。
- 最新自动化测试：401 项，395 通过，0 失败，6 项可选组件渲染测试因依赖未安装而跳过。
- 新增测试：分享四主题实际绘图调用、字段范围与布局边界、正文与小字对比度，以及全部 53 个第一方 WXML 的条件链。
- DevTools 实际载入了本工程，基础库 3.17.2；新访客直接进入发现页，My 不强迫先注册。
- 本地编辑并保存示例名片成功，3 个自定义标签和苏黎世城市持久化。
- 四种主题生成了真实 1200 × 960 微信分享图片；分享参数指向 `/pages/card-share/index`，不是本人编辑入口。
- 点击编辑器原生分享按钮，开发者工具直接出现“发送给：虚拟好友”面板，显示本轮实际卡片；未向真实联系人发送。
- 已发现并修复一次清理文案留下的孤立 `wx:else` 编译错误；增加条件链测试防止复发。
- 已修复新增装饰使用组件 WXSS 不支持的标签选择器的问题，改用类选择器。
- 原生分享绘图期间捕获到的小程序运行时异常：0（不包含修复前的编译诊断历史）。

## 待继续

电脑在原生分享面板检查后进入锁屏；已停止所有桌面操作。

- 系统深色模式全页面目视验收尚未完成。四种名片主题图片已经实际生成，但不等同于系统深色 UI 验收。
- 跨接收者冷启动的本轮实际操作尚未完成，现有隔离/篡改/并发自动化回归通过。
- 原生完整打包预览回执尚未生成；静态主包预算通过，不冒充平台回执。
- 最后类选择器与按钮宽度微调通过构建，仍待解锁后刷新并目视确认。
- 当前新工程中存在验收创建的虚构名片“林知遥”。原始本工程存储仅含运行状态，已备份；解锁后应恢复验收前存储，并以独立分享接收页展示示例效果。

## 本地证据

目录：`E:/Temp/abclub-145-classic-verification/`

- `share-card-ivory.png`、`share-card-ink.png`、`share-card-champagne.png`、`share-card-stone.png`：真实原生分享画布导出。
- `editor-saved-light.png`、`editor-light.png`：编辑与保存后的实际界面截图。
- `me-empty-light.png`：白金城市模块的实际界面截图（最后按钮宽度微调前）。
- `original-storage.private.json`：验收前存储备份，不纳入 Git。
- `synthetic-share-descriptor.private.json`：虚构示例的分享描述，不纳入 Git。
- `E:/Temp/abclub-classic-final-build.log`：完整本地构建结果。

## Windows 可复现性修复

Windows 自动换行曾导致已有 SVG/静态截图来源与云函数合同哈希不一致。已恢复对应文件原有 LF 字节，新增 `.gitattributes` 保持这些哈希输入跨平台一致；没有改动图片内容、合同内容或云函数业务逻辑。
