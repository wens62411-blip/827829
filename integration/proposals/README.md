# Contract change proposals

Feature modules do not edit frozen shared files directly. A proposal must name the requesting module, current contract version, exact affected actions/types/routes, backward-compatibility impact, migration plan, tests and both required owners (`foundation`, `integration`). Until accepted and a new contract version is frozen, the current 1.0.0 contract remains authoritative.

