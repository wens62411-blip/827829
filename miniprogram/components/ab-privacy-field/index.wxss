:host {
  display: block;
  min-width: 0;
  --privacy-accent: var(--ab-color-champagne-deep);
  --privacy-ink: var(--ab-color-ink);
  --privacy-muted: var(--ab-color-muted);
  --privacy-paper: var(--ab-color-paper);
  --privacy-line: var(--ab-color-border);
}

.privacy-field {
  box-sizing: border-box;
  min-width: 0;
  padding: var(--ab-space-4);
  border: 2rpx solid var(--privacy-line);
  border-radius: 4rpx;
  background: var(--privacy-paper);
  color: var(--privacy-ink);
}

.privacy-field__title-row {
  display: flex;
  min-width: 0;
  flex-wrap: wrap;
  gap: 12rpx;
  align-items: center;
}

.privacy-field__title {
  min-width: 0;
  overflow-wrap: anywhere;
  font-size: 34rpx;
  font-weight: 700;
}

.privacy-field__required,
.privacy-field__locked {
  padding: 2rpx 12rpx;
  border: 2rpx solid var(--privacy-line);
  border-radius: 999rpx;
  color: var(--privacy-muted);
  font-size: 22rpx;
}

.privacy-field__locked {
  border-color: var(--ab-color-gold-soft);
  color: var(--privacy-accent);
}

.privacy-field__helper {
  display: block;
  margin-top: var(--ab-space-1);
  color: var(--privacy-muted);
  font-size: var(--ab-font-caption);
  overflow-wrap: anywhere;
}

.privacy-field__editor,
.privacy-field__footer {
  display: block;
  margin-top: var(--ab-space-3);
}

.privacy-field__unknown,
.privacy-field__private-note {
  margin-top: var(--ab-space-3);
  padding: 18rpx var(--ab-space-3);
  border-left: 6rpx solid var(--ab-color-gold-soft);
  background: #f3ece2;
  color: var(--privacy-muted);
  font-size: 26rpx;
  line-height: 1.55;
}

.privacy-field__options {
  display: grid;
  min-width: 0;
  gap: 12rpx;
  margin-top: var(--ab-space-3);
}

.privacy-field__option {
  box-sizing: border-box;
  display: flex;
  width: 100%;
  min-width: 0;
  min-height: var(--ab-touch-target);
  align-items: center;
  justify-content: space-between;
  margin: 0;
  padding: 18rpx var(--ab-space-3);
  border: 2rpx solid var(--privacy-line);
  border-radius: var(--ab-radius-small);
  background: transparent;
  color: var(--privacy-ink);
  line-height: 1.45;
  text-align: left;
}

.privacy-field__option--selected {
  border-color: var(--privacy-accent);
  background: #f3ece2;
}

.privacy-field__option--disabled {
  opacity: 1;
}

.privacy-field__option-copy {
  min-width: 0;
  flex: 1;
}

.privacy-field__option-code,
.privacy-field__option-title,
.privacy-field__option-summary {
  display: block;
  min-width: 0;
  overflow-wrap: anywhere;
}

.privacy-field__option-code {
  color: var(--privacy-accent);
  font-size: 22rpx;
  font-weight: 700;
  letter-spacing: 1rpx;
}

.privacy-field__option-title {
  margin-top: 2rpx;
  font-size: 29rpx;
  font-weight: 600;
}

.privacy-field__option-summary {
  margin-top: 6rpx;
  color: var(--privacy-muted);
  font-size: 25rpx;
  line-height: 1.55;
}

.privacy-field__check {
  width: 48rpx;
  flex: 0 0 48rpx;
  margin-left: var(--ab-space-3);
  color: var(--privacy-accent);
  font-size: 34rpx;
  text-align: center;
}

@media (prefers-color-scheme: dark) {
  .privacy-field {
    --privacy-accent: var(--ab-color-gold-soft);
    --privacy-ink: var(--ab-color-dark-text);
    --privacy-muted: var(--ab-color-dark-muted);
    --privacy-paper: var(--ab-color-dark-surface);
    --privacy-line: var(--ab-color-dark-border);
    border-color: var(--privacy-line);
    background: var(--privacy-paper);
    color: var(--privacy-ink);
  }

  .privacy-field__required,
  .privacy-field__option {
    border-color: var(--privacy-line);
  }

  .privacy-field__unknown,
  .privacy-field__private-note,
  .privacy-field__option--selected {
    background: #2d2924;
  }

  .privacy-field__option,
  .privacy-field__option-title {
    color: var(--privacy-ink);
  }

  .privacy-field__helper,
  .privacy-field__option-summary,
  .privacy-field__unknown,
  .privacy-field__private-note {
    color: var(--privacy-muted);
  }
}

@media (prefers-reduced-motion: reduce) {
  .privacy-field__option {
    transition: none;
  }
}
