<view class="state" aria-role="region" aria-label="{{title}}">
  <text class="state__title">{{title}}</text>
  <text wx:if="{{description}}" class="state__description">{{description}}</text>
  <slot name="actions"></slot>
</view>

