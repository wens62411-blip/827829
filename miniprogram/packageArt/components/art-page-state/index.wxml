<view class="page-state" aria-role="status" aria-label="{{title}}"><view class="page-state__rule"></view><text class="page-state__eyebrow">{{kind}}</text><text class="page-state__title">{{title}}</text><text class="page-state__description">{{description}}</text><text wx:if="{{kind === 'LOADING' && weakNetwork}}" class="page-state__network">网络响应较慢，仍在读取公开内容；不会切换到合成示例。</text><button
    wx:if="{{retryable}}"
    class="page-state__retry ab-motion"
    hover-class="page-state__retry--pressed"
    bindtap="handleRetry"
  >重新读取</button></view>
